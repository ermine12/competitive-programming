from collections import Counter
class Solution:
    from collections import Counter
    #Function to check if a is a subset of b.
    def isSubset(self, a, b):
        countA = Counter(a)
        countB = Counter(b)
        return not countB-countA
    
    
