class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        lists = {}
        for i in range(len(nums)):
            j = target - nums[i]
            if j in lists:
                return [i,lists[j]]
            lists[nums[i]]=i 
            
